@page "/content/equipment"
@attribute [Authorize]

<PageTitle>Equipment</PageTitle>

<h1 id="top-equipment">Equipment</h1>

<p class="text-muted">Search, review, and maintain equipment entries from this screen.</p>

<div class="mb-3 d-flex gap-2 flex-wrap">
    <a class="btn btn-outline-secondary" href="/content">Back to Content Home</a>
    <a class="btn btn-outline-secondary" href="/content/import">Open Import</a>
</div>

<h6 class="mb-2">Related Content</h6>
<div class="mb-3 d-flex gap-2 flex-wrap">
    <a class="btn btn-outline-secondary" href="/content/monsters">Open Monsters</a>
    <a class="btn btn-outline-secondary" href="/content/treasure">Open Treasure</a>
    <span class="badge text-bg-primary align-self-center">Current: Equipment</span>
    <a class="btn btn-outline-secondary" href="/content/magic-items">Open Magic Items</a>
</div>
<small class="text-muted d-block mb-3">Use related links to continue cross-type content review after finishing this page.</small>

<div class="mb-3 d-flex gap-2 flex-wrap">
    <button class="btn btn-sm btn-outline-secondary" @onclick="StartCreateMode">Start New Entry</button>
    <a class="btn btn-sm btn-outline-secondary" href="/content/import">Open Import Workflow</a>
    <a class="btn btn-sm btn-outline-secondary" href="/content">Open Content Hub</a>
</div>

<div class="mb-3 d-flex gap-2 flex-wrap">
    <a class="btn btn-sm btn-outline-secondary" href="#top-equipment">Top</a>
    <a class="btn btn-sm btn-outline-secondary" href="#find-equipment">Find</a>
    <a class="btn btn-sm btn-outline-secondary" href="#list-equipment">List</a>
    <a class="btn btn-sm btn-outline-secondary" href="#details-equipment">Details</a>
    <a class="btn btn-sm btn-outline-secondary" href="#edit-equipment">Entry/Edit</a>
</div>
<small class="text-muted d-block mb-3">Recommended sequence: Find -> List -> Details -> Entry/Edit.</small>

<h5 id="find-equipment">Find Equipment</h5>
<p class="text-muted mb-2">Use search, category, and sort to narrow the list before opening details or edit mode.</p>

<div class="card mb-3">
    <div class="card-body">
        <div class="row g-2 align-items-end">
            <div class="col-md-5">
                <label class="form-label">Search</label>
                <InputText class="form-control" @bind-Value="searchQuery" placeholder="Search name, id, or category" />
            </div>
            <div class="col-md-3">
                <label class="form-label">Category</label>
                <InputSelect class="form-select" @bind-Value="categoryFilter">
                    <option value="">All</option>
                    @foreach (var category in AvailableCategories)
                    {
                        <option value="@category">@category</option>
                    }
                </InputSelect>
            </div>
            <div class="col-md-2">
                <label class="form-label">Sort</label>
                <InputSelect class="form-select" @bind-Value="sortOption">
                    <option value="@EquipmentSort.NameAsc">Name (A-Z)</option>
                    <option value="@EquipmentSort.NameDesc">Name (Z-A)</option>
                    <option value="@EquipmentSort.CategoryAsc">Category (A-Z)</option>
                    <option value="@EquipmentSort.CategoryDesc">Category (Z-A)</option>
                </InputSelect>
            </div>
            <div class="col-md-2">
                <button class="btn btn-outline-secondary w-100" @onclick="ClearFilters">Clear</button>
            </div>
        </div>
        <small class="text-muted d-block mt-2">Tip: combine category + search to reduce list size before selecting an item.</small>
    </div>
</div>

<h5 id="list-equipment">Equipment List Results</h5>
<div class="mb-2 d-flex gap-3 flex-wrap">
    <small class="text-muted">Visible: <strong>@VisibleEquipment.Count</strong></small>
    <small class="text-muted">Selected: <strong>@(string.IsNullOrWhiteSpace(selectedEquipmentId) ? "(none)" : selectedEquipmentId)</strong></small>
</div>
<div class="card">
    <div class="card-body">
        @if (VisibleEquipment.Count == 0)
        {
            <div class="alert alert-light border mb-0">No equipment matches current filters. Try clearing filters or broadening your search.</div>
        }
        else
        {
            <div class="table-responsive">
                <table class="table table-sm align-middle mb-0">
                    <thead>
                        <tr>
                            <th>Id</th>
                            <th>Name</th>
                            <th>Category</th>
                            <th>Type</th>
                            <th class="text-end">Cost</th>
                            <th class="text-end">Weight</th>
                            <th class="text-end">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in VisibleEquipment)
                        {
                            <tr class="@(string.Equals(item.Id, selectedEquipmentId, StringComparison.Ordinal) ? "table-primary" : string.Empty)">
                                <td><code>@item.Id</code></td>
                                <td>@item.Name</td>
                                <td>@item.Category</td>
                                <td>@item.Type</td>
                                <td class="text-end">@item.Cost</td>
                                <td class="text-end">@item.Weight</td>
                                <td class="text-end">
                                    <button class="btn btn-sm btn-outline-primary" @onclick="() => SelectEquipment(item.Id)">View</button>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>

<div class="mt-2 d-flex gap-2 flex-wrap">
    <a class="btn btn-sm btn-outline-secondary" href="#edit-equipment">Go to Entry/Edit</a>
    <a class="btn btn-sm btn-outline-secondary" href="#top-equipment">Back to Top</a>
</div>
        }
        <small class="text-muted d-block mt-2">Select any row and choose <strong>View</strong> to populate the detail panel below.</small>
    </div>
</div>

<h5 id="details-equipment" class="mt-4">Equipment Details</h5>
<p class="text-muted mb-2">Review read-only detail values before applying entry/edit changes.</p>
<div class="mb-2 d-flex gap-3 flex-wrap">
    <small class="text-muted">Selected: <strong>@(string.IsNullOrWhiteSpace(selectedEquipmentId) ? "(none)" : selectedEquipmentId)</strong></small>
    <small class="text-muted">Editor Mode: <strong>@(IsEditMode ? "Edit" : "Create")</strong></small>
</div>

<div class="card mt-3">
    <div class="card-body">
        <h5 class="card-title">Equipment Detail (Read-only)</h5>
        @if (SelectedEquipment is null)
        {
            <div class="alert alert-light border mb-0">Select an equipment entry from the list to view details.</div>
        }
        else
        {
            <dl class="row mb-0">
                <dt class="col-sm-3">Id</dt>
                <dd class="col-sm-9"><code>@SelectedEquipment.Id</code></dd>

                <dt class="col-sm-3">Name</dt>
                <dd class="col-sm-9">@SelectedEquipment.Name</dd>

                <dt class="col-sm-3">Category</dt>
                <dd class="col-sm-9">@SelectedEquipment.Category</dd>

                <dt class="col-sm-3">Type</dt>
                <dd class="col-sm-9">@SelectedEquipment.Type</dd>

                <dt class="col-sm-3">Cost</dt>
                <dd class="col-sm-9">@SelectedEquipment.Cost</dd>

                <dt class="col-sm-3">Weight</dt>
                <dd class="col-sm-9">@SelectedEquipment.Weight</dd>

                <dt class="col-sm-3">Weight Unit</dt>
                <dd class="col-sm-9">@SelectedEquipment.WeightUnit</dd>

                <dt class="col-sm-3">Description</dt>
                <dd class="col-sm-9">@SelectedEquipment.Description</dd>

                <dt class="col-sm-3">Source</dt>
                <dd class="col-sm-9">@SelectedEquipment.Source</dd>

                <dt class="col-sm-3">Tags</dt>
                <dd class="col-sm-9">@SelectedEquipment.Tags</dd>

                <dt class="col-sm-3">Extensions</dt>
                <dd class="col-sm-9">
                    @if (SelectedEquipment.Extensions.Count == 0)
                    {
                        <span class="text-muted">(none)</span>
                    }
                    else
                    {
                        <ul class="mb-0">
                            @foreach (var ext in SelectedEquipment.Extensions)
                            {
                                <li><code>@ext.Key</code>: @ext.Value</li>
                            }
                        </ul>
                    }
                </dd>
            </dl>
        }
    </div>
</div>

<h5 id="edit-equipment" class="mt-4">Equipment Entry/Edit</h5>
<p class="text-muted mb-2">Use this section to create new equipment or edit the currently selected entry.</p>

<div class="card mt-3">
    <div class="card-body">
        <div class="d-flex justify-content-between align-items-center mb-2">
            <h5 class="card-title mb-0">Equipment Entry/Edit</h5>
            <span class="badge @(IsEditMode ? "text-bg-primary" : "text-bg-secondary")">@(IsEditMode ? "Edit" : "Create")</span>
        </div>

        <div class="row g-2">
            <div class="col-md-4">
                <label class="form-label">Id (required)</label>
                <InputText class="form-control" @bind-Value="editId" />
            </div>
            <div class="col-md-4">
                <label class="form-label">Name (required)</label>
                <InputText class="form-control" @bind-Value="editName" />
            </div>
            <div class="col-md-4">
                <label class="form-label">Cost (>= 0)</label>
                <InputNumber class="form-control" @bind-Value="editCost" />
            </div>
            <div class="col-md-4">
                <label class="form-label">Weight (>= 0)</label>
                <InputNumber class="form-control" @bind-Value="editWeight" />
            </div>
            <div class="col-md-4">
                <label class="form-label">Weight Unit</label>
                <InputText class="form-control" @bind-Value="editWeightUnit" />
            </div>
            <div class="col-md-4">
                <label class="form-label">Source</label>
                <InputText class="form-control" @bind-Value="editSource" />
            </div>
            <div class="col-md-4">
                <label class="form-label">Tags</label>
                <InputText class="form-control" @bind-Value="editTags" />
            </div>
            <div class="col-12">
                <label class="form-label">Description</label>
                <InputTextArea class="form-control" @bind-Value="editDescription" rows="2" />
            </div>
        </div>

        <div class="mt-2">
            <EquipmentCategoryTypeSelector
                Category="@editCategory"
                CategoryChanged="OnEditorCategoryChanged"
                Categories="@AvailableCategories"
                Type="@editType"
                TypeChanged="OnEditorTypeChanged"
                Types="@AvailableTypes" />
        </div>
        <small class="text-muted d-block mt-2">Use category/type selector to keep equipment taxonomy consistent.</small>

        <div class="card mt-3">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center mb-2">
                    <h6 class="mb-0">Ruleset Extensions (AD&amp;D1 / BRP)</h6>
                    <button class="btn btn-sm btn-outline-secondary" @onclick="AddExtensionRow">Add Extension</button>
                </div>
                <small class="text-muted d-block mb-2">Use extensions for ruleset-specific fields that are not part of the core equipment model.</small>

                @if (extensionRows.Count == 0)
                {
                    <div class="alert alert-light border mb-0">No extensions configured. Add rows for ruleset-specific equipment detail fields.</div>
                }
                else
                {
                    <div class="table-responsive">
                        <table class="table table-sm align-middle mb-0">
                            <thead>
                                <tr>
                                    <th>Key (required when value set)</th>
                                    <th>Value</th>
                                    <th class="text-end">Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                @for (var i = 0; i < extensionRows.Count; i++)
                                {
                                    var row = extensionRows[i];
                                    <tr>
                                        <td><InputText class="form-control" @bind-Value="row.Key" /></td>
                                        <td><InputText class="form-control" @bind-Value="row.Value" /></td>
                                        <td class="text-end">
                                            <button class="btn btn-sm btn-outline-danger" @onclick="() => RemoveExtensionRow(i)">Remove</button>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                }
            </div>
        </div>

        <div class="d-flex gap-2 mt-3">
            <button class="btn btn-outline-secondary" @onclick="ValidateEditorForm">Validate Form</button>
            <button class="btn btn-primary" @onclick="SaveEquipment">Save</button>
            <button class="btn btn-outline-warning" @onclick="CancelEdit">Cancel</button>
            <button class="btn btn-outline-secondary" @onclick="StartCreateMode">New (Create Mode)</button>
        </div>

        <small class="text-muted d-block mt-2">Validate is optional pre-check. Save is the primary action for persisting changes.</small>
        <small class="text-muted d-block">Use <strong>New</strong> to clear editor values and start a fresh entry after save/review.</small>
        <small class="text-muted d-block">Use <strong>Cancel</strong> to leave edit mode without persisting form changes.</small>

        @if (!string.IsNullOrWhiteSpace(editorValidationMessage))
        {
            <div class="alert @(editorValidationPassed ? "alert-success" : "alert-danger") mt-3 mb-0">
                @editorValidationMessage
            </div>
        }

        @if (!string.IsNullOrWhiteSpace(editorSaveMessage))
        {
            <div class="alert @(editorSaveSucceeded ? "alert-success" : "alert-danger") mt-2 mb-0">
                @editorSaveMessage
            </div>
        }

        <div class="mt-3 d-flex gap-2 flex-wrap">
            <a class="btn btn-sm btn-outline-secondary" href="#find-equipment">Back to Find</a>
            <a class="btn btn-sm btn-outline-secondary" href="#list-equipment">Back to List</a>
            <a class="btn btn-sm btn-outline-secondary" href="#details-equipment">Back to Details</a>
            <a class="btn btn-sm btn-outline-secondary" href="#top-equipment">Back to Top</a>
            <a class="btn btn-sm btn-outline-secondary" href="/content/import">Open Import</a>
            <a class="btn btn-sm btn-outline-secondary" href="/content/magic-items">Next: Magic Items</a>
            <a class="btn btn-sm btn-outline-secondary" href="/content">Open Content Hub</a>
        </div>
    </div>
</div>
